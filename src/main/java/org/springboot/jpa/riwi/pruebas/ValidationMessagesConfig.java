package org.springboot.jpa.riwi.pruebas;

import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.PropertySource;

@Configuration
@PropertySource(value = "classpath:messages.properties", encoding = "UTF-8")
public class ValidationMessagesConfig {
    //Atributos de ValidationMessagesConfig
    //Constructores de ValidationMessagesConfig
    //Asignadores de atributos de ValidationMessagesConfig (setters)
    //Lectores de atributos de ValidationMessagesConfig (getters)
    //Métodos de ValidationMessagesConfig
}
