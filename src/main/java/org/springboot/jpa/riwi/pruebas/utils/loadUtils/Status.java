package org.springboot.jpa.riwi.pruebas.utils.loadUtils;

public enum Status {
    PENDING,
    IN_TRANSIT,
    DELIVERED;
    
    //Atributos de Status
    //Constructores de Status
    //Asignadores de atributos de Status (setters)
    //Lectores de atributos de Status (getters)
    //Métodos de Status
}
